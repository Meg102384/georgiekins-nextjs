# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# env files (secrets — never commit these)
.env
.env*.local

# misc
.DS_Store
*.pem
npm-debug.log*

# fulfillment PDFs are digital product deliverables, not source
# (uncomment if you don't want them in version control)
# /fulfillment-pdfs/*.pdf
